<%= form_for(@mission) do |f| %>
  <% if @mission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mission.errors.count, "error") %> prohibited this mission from being saved:</h2>

      <ul>
      <% @mission.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mission %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.check_box :is_public %>
    <%= f.label :public_can_view %>
  </div>
  <div class="field">
    <%= f.label :about_this_mission %><br />
    <%= f.text_area :blurb %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
