        new_user_session GET    /users/sign_in(.:format)                             devise/sessions#new
            user_session POST   /users/sign_in(.:format)                             devise/sessions#create
    destroy_user_session DELETE /users/sign_out(.:format)                            devise/sessions#destroy
           user_password POST   /users/password(.:format)                            devise/passwords#create
       new_user_password GET    /users/password/new(.:format)                        devise/passwords#new
      edit_user_password GET    /users/password/edit(.:format)                       devise/passwords#edit
                         PUT    /users/password(.:format)                            devise/passwords#update
cancel_user_registration GET    /users/cancel(.:format)                              devise/registrations#cancel
       user_registration POST   /users(.:format)                                     devise/registrations#create
   new_user_registration GET    /users/sign_up(.:format)                             devise/registrations#new
  edit_user_registration GET    /users/edit(.:format)                                devise/registrations#edit
                         PUT    /users(.:format)                                     devise/registrations#update
                         DELETE /users(.:format)                                     devise/registrations#destroy
                   users GET    /users(.:format)                                     users#index
                         POST   /users(.:format)                                     users#create
                new_user GET    /users/new(.:format)                                 users#new
               edit_user GET    /users/:id/edit(.:format)                            users#edit
                    user GET    /users/:id(.:format)                                 users#show
                         PUT    /users/:id(.:format)                                 users#update
                         DELETE /users/:id(.:format)                                 users#destroy
       mission_successes GET    /missions/:mission_id/successes(.:format)            successes#index
                         POST   /missions/:mission_id/successes(.:format)            successes#create
     new_mission_success GET    /missions/:mission_id/successes/new(.:format)        successes#new
    edit_mission_success GET    /missions/:mission_id/successes/:id/edit(.:format)   successes#edit
         mission_success GET    /missions/:mission_id/successes/:id(.:format)        successes#show
                         PUT    /missions/:mission_id/successes/:id(.:format)        successes#update
                         DELETE /missions/:mission_id/successes/:id(.:format)        successes#destroy
         mission_drivers GET    /missions/:mission_id/drivers(.:format)              drivers#index
                         POST   /missions/:mission_id/drivers(.:format)              drivers#create
      new_mission_driver GET    /missions/:mission_id/drivers/new(.:format)          drivers#new
     edit_mission_driver GET    /missions/:mission_id/drivers/:id/edit(.:format)     drivers#edit
          mission_driver GET    /missions/:mission_id/drivers/:id(.:format)          drivers#show
                         PUT    /missions/:mission_id/drivers/:id(.:format)          drivers#update
                         DELETE /missions/:mission_id/drivers/:id(.:format)          drivers#destroy
     mission_constraints GET    /missions/:mission_id/constraints(.:format)          constraints#index
                         POST   /missions/:mission_id/constraints(.:format)          constraints#create
  new_mission_constraint GET    /missions/:mission_id/constraints/new(.:format)      constraints#new
 edit_mission_constraint GET    /missions/:mission_id/constraints/:id/edit(.:format) constraints#edit
      mission_constraint GET    /missions/:mission_id/constraints/:id(.:format)      constraints#show
                         PUT    /missions/:mission_id/constraints/:id(.:format)      constraints#update
                         DELETE /missions/:mission_id/constraints/:id(.:format)      constraints#destroy
           mission_facts GET    /missions/:mission_id/facts(.:format)                facts#index
                         POST   /missions/:mission_id/facts(.:format)                facts#create
        new_mission_fact GET    /missions/:mission_id/facts/new(.:format)            facts#new
       edit_mission_fact GET    /missions/:mission_id/facts/:id/edit(.:format)       facts#edit
            mission_fact GET    /missions/:mission_id/facts/:id(.:format)            facts#show
                         PUT    /missions/:mission_id/facts/:id(.:format)            facts#update
                         DELETE /missions/:mission_id/facts/:id(.:format)            facts#destroy
     mission_assumptions GET    /missions/:mission_id/assumptions(.:format)          assumptions#index
                         POST   /missions/:mission_id/assumptions(.:format)          assumptions#create
  new_mission_assumption GET    /missions/:mission_id/assumptions/new(.:format)      assumptions#new
 edit_mission_assumption GET    /missions/:mission_id/assumptions/:id/edit(.:format) assumptions#edit
      mission_assumption GET    /missions/:mission_id/assumptions/:id(.:format)      assumptions#show
                         PUT    /missions/:mission_id/assumptions/:id(.:format)      assumptions#update
                         DELETE /missions/:mission_id/assumptions/:id(.:format)      assumptions#destroy
                missions GET    /missions(.:format)                                  missions#index
                         POST   /missions(.:format)                                  missions#create
             new_mission GET    /missions/new(.:format)                              missions#new
            edit_mission GET    /missions/:id/edit(.:format)                         missions#edit
                 mission GET    /missions/:id(.:format)                              missions#show
                         PUT    /missions/:id(.:format)                              missions#update
                         DELETE /missions/:id(.:format)                              missions#destroy
              home_index GET    /home/index(.:format)                                home#index
                    root        /                                                    home#index
